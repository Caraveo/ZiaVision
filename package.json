{
  "name": "ziavision",
  "version": "1.0.0",
  "private": true,
  "description": "ZiaVision - Innovative Solutions",
  "main": "src/server.js",
  "scripts": {
    "start": "cd client && npm start",
    "dev": "nodemon server.js",
    "test": "cd client && npm test",
    "build": "cd client && npm install && npm run build",
    "lint": "eslint .",
    "client": "cd client && npm start",
    "install-all": "npm install && cd client && npm install",
    "deploy": "npm run install-all && npm run build && npm start",
    "vercel-build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "helmet": "^7.0.0",
    "compression": "^1.7.4",
    "express-rate-limit": "^6.9.0",
    "winston": "^3.10.0",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.1",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.6.4",
    "eslint": "^8.47.0",
    "concurrently": "^8.2.0",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 